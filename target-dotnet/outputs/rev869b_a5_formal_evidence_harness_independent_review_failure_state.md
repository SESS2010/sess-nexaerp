# REV869B A5 Formal Evidence Harness Independent Review Failure State

## Record identity and terminal decision

- Reviewed harness commit: `687a75ee7e9cdf7efb6e66e51547872ab3ed196a`
- Review role: independent report-only reviewer
- Review result: three Critical findings and four High findings
- Terminal rule: this failed review is terminal for this harness candidate. A corrected rerun cannot convert it to PASS. Any future correction requires a new management-authorized harness candidate and a new independent review identity.

## Canonical states

```text
A5_FORMAL_EVIDENCE_HARNESS_REVIEW_STATE=FAIL
A5_FORMAL_EVIDENCE_HARNESS_INTERNAL_VALIDATION_CLAIM=UNSUPPORTED
A5_SOURCE_IMPLEMENTATION_GATE=NO_GO
A5_HARNESS_REMEDIATION_GATE=NO_GO_PENDING_MANAGEMENT_DECISION
```

Reason for `A5_FORMAL_EVIDENCE_HARNESS_INTERNAL_VALIDATION_CLAIM=UNSUPPORTED`: the runner emits no mutant events; internal validation exercised synthetic journals, not runner output.

## Independent review findings table

| ID | Severity | Dimension | File+line | Issue | Why it matters | Suggested remediation |
|---|---|---|---|---|---|---|
| A5H-001 | Critical | Evidence integrity / operator error | `Invoke...ps1:254-266,291-319`; `Verify...ps1:98-128,149-159`; plan `:13-30` | Expected test arithmetic is never recomputed. Runner leaves `passed`, `failed`, `skipped`, and `total` null. Verifier checks exit code and duplicate TRX IDs only. It does not compare test counts/outcomes with the plan. Executable, arguments, working directory, tool identity, and expected result path are not independently compared with authorization. Stage order is also not enforced. | Exit-zero or no-op commands can stand in for required test stages. Empty or incomplete TRX evidence can be accepted. This permits a false PASS without the required 30/30, 23/23, 116/116, 81/81, or 455/455 results. | Freeze exact commands per stage, independently compare every command field, require result artifacts by stage kind, parse all outcomes, and recompute exact plan arithmetic. Enforce ordinal and subordinal ordering. |
| A5H-002 | Critical | Secrets, privilege, prohibited operations | `Invoke...ps1:324-332`; `Verify...ps1:140-148` | Prohibited-operation counters are not measured. The runner creates empty instrumentation arrays after execution and unconditionally assigns every counter zero. | PostgreSQL, migrations, services, deployment, production access, or network activity could occur while the evidence still reports zero. The verifier merely validates the hash of this fabricated empty inventory. | Obtain counters from independently enforced OS/process/network/database telemetry. Fail if monitoring is unavailable. Bind process descendants and observed operations to the run. |
| A5H-003 | Critical | Threat model / fabricated PASS | `Invoke...ps1:120-128,210-245`; `Verify...ps1:46-90` | The authorization hash is supplied by the same caller as the authorization file. Journal hashes are unkeyed. Locks and evidence are ordinary writable files. There is no trusted signature, external transparency log, append-only store, or independent authorization trust anchor. | A motivated developer can create a new authorization record and matching hash, fabricate and re-chain evidence, or delete locks and rerun. The hash chain detects accidental changes but does not prove provenance. | Require a management-signed authorization envelope with a pinned public key, immutable remote evidence storage, signed events, and externally durable attempt/candidate uniqueness. |
| A5H-004 | High | Mutant integrity | `Invoke...ps1:271-333`; `Verify...ps1:129-139`; plan `:32-72` | The runner contains no path that emits `MUTANT_STARTED` or `MUTANT_COMPLETED`. The verifier accepts any 40 unique self-reported IDs and booleans; it does not require the exact 40 plan IDs or verify mutated blobs, build output, killer tests, or restoration from Git. | Legitimate runner-produced evidence appears unable to satisfy the verifier, while a fabricated journal can supply 40 asserted mutant successes. | Implement first-class mutant execution/evidence events and verify exact plan IDs, source locations, original/mutated/restored hashes, build evidence, killer results, and restoration from committed blobs. |
| A5H-005 | High | Immutability / plan freeze | `Invoke...ps1:210-229,267-268`; `Verify...ps1:50-53,118-127` | Runner initially hashes plan/schema against authorization, but verifier checks only the schema title. It does not verify plan hash, schema hash, harness aggregate, or verifier identity against authorization. Events do not contain the plan/schema hashes. | Plan, schema, or verifier drift between preflight and verification can go undetected. A shared mutable plan can redefine what the verifier accepts. | Record all hashes in the immutable start event and have the verifier independently compare them with signed authorization and committed blobs. |
| A5H-006 | High | Candidate substitution / freeze | `Invoke...ps1:283-322`; `Verify...ps1:91-95` | Formal command working directories are unrestricted. After each command, runner checks only dirty status, not HEAD or tree identity. A clean reset to another commit is not detected during execution, and source can be restored before final verification. | Commands need not execute against the authorized candidate. Temporary candidate substitution can evade the final identity check. | Require every working directory to be inside the candidate worktree. Recheck HEAD, tree, detached state, manifest blobs, and relevant tool files before and after every command. Use a read-only worktree or immutable snapshot. |
| A5H-007 | High | Retry and abort semantics | `Invoke...ps1:130-138,238-252,277-281` | Candidate/run/failure locks are local deletable files. Handled command failures are journaled before the failure lock, but process termination or an unhandled exception can leave an incomplete attempt without a terminal event. | A motivated operator can remove the evidence root or locks. A crash produces ambiguous evidence rather than a durable terminal failure. | Register the attempt externally before execution, use write-once storage, and treat every opened attempt lacking a signed success as permanently failed. Add top-level crash/finally recording. |
| A5H-008 | Medium | Schema completeness | schema `:34-45,91-163`; verifier `:52-64` | The JSON Schema is never evaluated. Only its title and a manually copied top-level property list are checked. The schema also lacks event-type-specific rules and permits null results/counts on completed test events. | Malformed nested objects and semantically incomplete events may reach casts or be accepted. Cardinality alone does not validate a schema. | Use a pinned Draft 2020-12 validator and add conditional schemas for every event type and stage kind. Make required result fields non-null where applicable. |
| A5H-009 | Medium | Evidence containment | `Verify...ps1:37-44` | Evidence containment uses string `StartsWith($RunRoot)` without a trailing separator boundary. | A sibling path such as `C:\run-other` can satisfy a prefix check for `C:\run`. | Resolve canonical paths and require equality with the root or a prefix of `RunRoot + DirectorySeparatorChar`. |
| A5H-010 | Medium | Determinism | `Invoke...ps1:50-58,63-79,109-117,283-297`; validator `:206-227` | Run IDs, UTC/local timestamps, timezone, monotonic frequency, process IDs, absolute paths, tool metadata, locale, line endings, and machine state vary. The validator compares a reduced semantic projection that deliberately omits these fields. | Two machines will not produce byte-identical evidence. Commands may also depend on locale, random seeds, child parallelism, or unsanitized environment variables. | Define determinism as semantic rather than byte-identical, normalize paths/locale/timezone, pin seeds and concurrency, and list all excluded nondeterministic fields explicitly. |
| A5H-011 | Medium | Secrets and privilege | `Invoke...ps1:43-58,291-300` | No hardcoded credential was found, but environment scrubbing covers only selected prefixes. Arguments, display text, stdout, and stderr are retained without redaction. The plan’s `allowedExecutables` list is never enforced. | Tokens or connection strings can leak into evidence. Arbitrary executables and inherited privileges can be used. Lifecycle-administrator privilege is not required by the harness itself, but it is not prevented in child commands. | Enforce the executable allowlist, construct a minimal environment, reject secret-bearing arguments, redact outputs, and run under a dedicated least-privilege account. |
| A5H-012 | Medium | Verifier independence | runner `:28-33,140-155,334-350`; verifier `:16-35,85-128` | Verifier is a separate script but shares canonicalization, hash conventions, schema assumptions, authorization, plan, filesystem, candidate worktree, machine, and account with the runner. Runner invokes it directly. | A shared assumption, compromised PowerShell/Git/toolchain, or writable evidence store can affect both sides identically. Independence is logical, not administrative or cryptographic. | Execute verification in a separate trust domain from read-only evidence, using independently implemented parsing and pinned verifier identity. |
| A5H-013 | Info | Coverage gap | All six files | The harness does not establish ERP business correctness, PostgreSQL behavior, migration safety in a real database, UI correctness, deployment readiness, UAT, resilience, or production readiness. | A harness PASS would only attest to the defined source-only evidence process. | Preserve separate Phase-A, Phase-B, PostgreSQL, UAT, and production go-live gates. |

## Retained prohibitions

- No harness source file was changed.
- No finding remediation was started.
- The harness was not executed, re-reviewed, or re-validated.
- The independent review verdict remains `HARNESS_REVIEW_STATE=FAIL` and is not reopened or amended.
- A5 source implementation remains prohibited.
