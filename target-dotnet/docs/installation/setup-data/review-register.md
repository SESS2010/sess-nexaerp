# Assisted setup review register - complete per DEMO/production run

Server agent target-database confirmation, installed service/config reference and date:

Source commit / API origin / reviewed input hashes:

Operator roster (employee codes, roles, realm, company/department scopes; no passwords/tokens):

| Company | Category | Maker receipt ID/hash | TD Read receipt hash | Three same-warehouse condition locations correct? | Effective for intended use through 8 October? | TD employee, date, signature/reference |
|---|---|---|---|---|---|---|
| SESS_PROPRIETORSHIP | ELE | | | | | |
| SESS_PROPRIETORSHIP | FAB | | | | | |
| SESS_PROPRIETORSHIP | REF | | | | | |
| SESS_PVT_LTD | ELE | | | | | |
| SESS_PVT_LTD | FAB | | | | | |
| SESS_PVT_LTD | REF | | | | | |

Attach a condition-location sheet naming company, warehouse, bin, condition, effective
range, actual record ID, original input hash, Stores maker receipt and independent TD
read/review. Check each opening-stock AVAILABLE bin, not only the default receiving bin.
Neither condition locations nor routes has an API approval stage; these signed reviews
are operational controls. The reviewer must be a different employee from the maker. Use a fresh evidence folder
for each independent review and archive the actual Read JSON with this signed register;
a later Read in the same folder replaces that kind/company snapshot.

Attach API approval evidence separately for warehouse/rack submission and TD approval,
Accounts-created GST and independent TD/MD approval, and Purchase-created qualifications
with independent TD verification then MD approval. Name actual employees, records and
versions; a generic role label alone is not a review receipt. Check vendor certificate
readability, Accounts commercial verification and MD final approval through the screens.

Record read-only stock movement counts before opening: configuration did not create stock.
Record both POSTED template-v2 opening receipts before ordinary transactions are enabled.
No test GRN, issue or adjustment is permitted on production before BOTH ceremonies.

Exceptions, uncertain PENDING journals and their reviewed resolution (never erase evidence):

TD acceptance / date:
