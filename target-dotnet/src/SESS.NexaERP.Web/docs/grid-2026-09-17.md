# Frontend grid — 17 September 2026

Backend: origin/main `605faf9` (four commits over 591fe42: Keycloak recovery
060132f, intercompany 610894e, **QC policy approval + serialized recovery
4e99257**, **UOM precision 605faf9**). Local DB migrated 103 → 106 today as
`nexa_erp_migration` (IntercompanyRoutes, IntercompanyPurchasePublication,
QcPolicyDecisions); principals `status` VERIFIED without re-provisioning.
Frontend: feature/frontend. Company: SESS_PVT_LTD. API as `nexa_erp_runtime`
on :5000, frontend on :5175. Every action below was driven through the screen
unless the line says "API".

Scope today: the manager's **12 → 20** on the pushed fixes. Lines 1–11, 21, 22
are carried from 16 Sep unchanged (their fixes #1–#4, #7 are still open).

**E2E** = runs end to end, as the right person, against the real API:
`YES` / `YES*` (passes only under a flagged dev workaround) / `NO` / `NOT RUN`.

| # | Screen | Logins used | 17 Sep E2E | Evidence / blocking reason |
|---|---|---|---|---|
| 12 | QC queue + inspect | SESS-33 inspect, SESS-01 approve policy (API) | **YES\*** | Policy `6e36ed91` VISUAL/NOS sample 1 for item 1SAM250000R1004 created as SESS-33 (`QC_MANAGER`) and **approved as SESS-01 through the new `/approve` endpoint** — #6(a) closed. Queue then shows `Policy` for GRN-26-27-000011 lot 1. Inspect page (new today: policy read + per-sample PASS/FAIL capture) finalized the **serialized** lot 1 accepted / 1 rejected, VISUAL = FAIL → `QCI-20260917-000011` PARTIAL_ACCEPTED, stock posted (batch c13d0efc) — #6(b) closed. Still flagged: SESS-33 needs `STORES_ASSISTANT` SUPPORT for the GRN read (#7); no policy admin screen exists, so approval is API-only. |
| 13 | Concession | SESS-33 raise, SESS-01 approve | YES | `CON-20260917-000011` raised from the inspection's FAIL row (lot disposition, quantity and rejected serial now prefilled from `GET /qc/inspections/{n}` — backend cc446ab, screen caught up today) → DRAFT → TD **APPROVED** into TRIAL-C01-GEN-01, batch 6fb2e0b7, provenance annotation recorded. |
| 14 | Stock Check | SESS-15 | YES | 16 Sep evidence stands; requisition list 200 on 106/106. |
| 15 | Material Issue Request | SESS-25, SESS-41 | YES | MIR-…-000051 APPROVED (16 Sep); list 200 on 106/106. |
| 16 | Material Issue (by scan) | SESS-35 | YES | `MI-SESS_PVT_LTD-26-27-000011` from MIR-000051 to SESS-25: both MPCB serials (accepted 0001 + concession 0002) issued, stock posting 4e72eff2, return due 18 Sep. Scan box: "Add without scan" + quantity 2. |
| 17 | Material Return | SESS-25 declare, SESS-35 accept | YES | `MR-SESS_PVT_LTD-26-27-000011` for serial MPCB-0916-0002 (returned 1 / consumed 0 / held 0) → SUBMITTED → Stores **ACCEPTED**; issue PARTIALLY_RETURNED, outstanding custody empty on the server. Frontend fix today: serialized lines take the **issued serial** as scan code (the server checks `StoredSerialNumber`; "declare without scan" sent the item code and was refused). |
| 18 | Customer PO | SESS-01 | YES | CPO-00003 (16 Sep); list 200 on 106/106. |
| 19 | Job Order | SESS-25, SESS-14, SESS-33 | YES | JO-000031 OPEN (16 Sep); list 200 on 106/106. |
| 20 | Component Fitment / Actual BOM | SESS-25 | YES | `FIT-SESS_PVT_LTD-2026-000001`: issue line 88d65a7a (serial MPCB-0916-0001) fitted to JO-000031, CONFIRMED, PRODUCTION_MANAGER basis. **Actual BOM 8c763fb3 generated** with one FITMENT entry (lot 0bb57716, serial 649fe978). Accepted value ₹0 — no vendor bill exists (Codex's open finding: Accounts cannot retrieve GRN line ids). Screen still asks for the issue-line GUID by hand. |

**Count: 21 / 22 proven — 15 clean, 6 under flagged workarounds (7–12). 1 NO (line 1 item edit — #5 is pushed but not re-run today). 0 NOT RUN. Chain 12 → 20 is closed end to end for the first time: GRN → QC → concession → issue → return → fitment → Actual BOM.**

## Backend findings (in chain order)

Closed today: **#6(a)** QC policy approval, **#6(b)** serialized discrepancy finalize.
Still open from 16 Sep: #1 CRLF (in Codex's worktree, uncommitted), #2 migration 75,
#3 technical verification scope, #4 GRN 3-char category, #7 grant gaps, #8 GST rule.
#5 UOM precision is pushed (605faf9) and not yet re-run against line 1.

New today:

9. **`MaterialIssueLineView` has no serial number or fitted quantity.** The
   return service demands the exact `StoredSerialNumber` as the scan code
   (`EfMaterialIssueService.Return.cs:79`), but the issue view exposes only
   `InventorySerialId`, so no screen can tell which serial belongs to which line
   — the return form now takes the serial as typed and lets the server refuse a
   mismatch. Same view carries no consumed/fitted quantity, so an issue's
   outstanding per line can only be rebuilt from the fitment list, which Stores
   (SESS-35) gets 403 on (`production.component-fitments` view) — Stores cannot
   see that the material it issued has been fitted. Same class as Codex's
   "Accounts cannot retrieve GRN line ids".
10. **QC policy administration has no screen** and `GET /qc-inspection-policies`
    ANDs `itemId`/`categoryId`, so the inspect page fetches both bindings and
    merges. Fine for now; a policy list/approve page is needed before go-live.

## Frontend changes today (feature/frontend)

- QC inspect + correction: read effective policies (item, or category with no
  item) and capture one observation + PASS/FAIL per required sample; numeric
  observations are checked against the approved limits before the round trip.
  Replaces the "no read endpoint" block.
- Concession raise: lot disposition id, rejected quantity and rejected serial
  ids prefilled from the inspection (`QcInspectionLotDispositionId`,
  `InventorySerialIds` typed).
- Material return: serialized lines take the issued serial as scan code
  ("Enter serial" per line; item-code scans are refused client-side).
- Material issue detail: **Fitted** column and outstanding = issued − returned
  − fitted (from the job order's active fitments, when readable).

## Run log

- 17 Sep 12:05 backend worktree fast-forwarded 591fe42 → 605faf9 (235 CRLF-only
  local mods, no overlap); 12:10 `dotnet ef database update` 104 → 106 Done as
  nexa_erp_migration; principals status VERIFIED; 12:15 API up as runtime
  (runtime and migration role passwords reset locally; user-secrets updated).
- Grid from 12:17: policy 6e36ed91 → QCI-20260917-000011 → CON-20260917-000011
  → MI-…-000011 → MR-…-000011 → FIT-…-000001 / Actual BOM 8c763fb3.
