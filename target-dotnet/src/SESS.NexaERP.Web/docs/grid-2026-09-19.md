# Frontend grid — 19 September 2026

Backend: origin/main `f94f332` (the manager's `64f5acf` set plus seven later
commits, up to "Record observed authority recheck"). Local DB migrated
**106 → 113**, provision RECONCILED, status VERIFIED, API as `nexa_erp_runtime`
on :5000 from the main worktree, frontend on :5175. Every action below was
driven through the screen unless the line says "API".

Scope today, in the manager's order: (4) Actual BOM and dossier against the
−1,250 prediction, (3) vendor bill screen then item 5, (2) re-run 7–12 without
the dev workarounds, (1) line 24 in SESS_PROPRIETORSHIP then item 6.

**E2E** = runs end to end, as the right person, against the real API:
`YES` / `YES*` (passes only under a flagged dev workaround) / `NO` / `NOT RUN`.

| # | Screen | Logins used | 19 Sep E2E | Evidence |
|---|---|---|---|---|
| 2 | Vendor master (Purchase read) | SESS-15 | **YES** (was seed gap) | RFQ vendor picker as PURCHASE_MANAGER returns `SESS-V-0001 6 SIGMA ENTERPRISES` (`GET /masters/vendors` 200 — was 403 on 16 Sep). #7 `bfc92cb` / migration 112. |
| 5 | Purchase Requisition | SESS-15 raise, SESS-14 verify + approve | YES | PR-2026-27-000009 (2 × MPCB, ₹2,500): Submitted → PendingApproval → StockCheckPending, route DEPARTMENT_ONLY. Stock check as SESS-15: 1 on hand (the 17 Sep return), reserved 1, **handoff 1** → PHO-PR-2026-27-000009-001. API. |
| 6 | RFQ | SESS-15 | **YES** (was YES* on the vendor picker) | RFQ-26-27-000021 Draft → invite SESS-V-0001 via the picker → Issued. API. |
| 7 | Quotation + technical verification | SESS-15 record, **SESS-01 verify** | **YES** (was YES*) | VQ-26-27-000021 recorded (1 × 1,250, HSN 85362010 TN/TN 18 %, `6S/Q/2026/0919`) → SESS-01 (`TECHNICAL_DIRECTOR`, in the contract, no SUPPORT role) verified → TechnicallyCompliant. **New finding 17:** SESS-04 (`TECHNICAL_SUPPORT_MANAGER`) — the role #3 was about — reads the quotation (new TSM grant works) and passes the new own-department scope predicate, then the write dies in the database: `rev869b_history_actor_binding` 42501 → **500**. |
| 8 | Comparison | SESS-15 build/recommend, SESS-14 approve | **YES** (was YES*) | CMP-26-27-000021 → recommended → Approved v2. API. |
| 9 | Purchase Order | SESS-15 create/submit/issue, SESS-14 approve | **YES** (was YES*) | PO-26-27-000021 Draft → PendingApproval (DEPARTMENT_ONLY) → Approved → Issued v3. API. |
| 10 | Gate Entry | SESS-35 | **YES** (was YES*) | GE-26-27-000021 (DC `6S/DC/0919/002`, Road, ISO checks) → FINALIZED. API. |
| 11 | Goods Receipt | SESS-35 | **YES\*** (unchanged) | GRN-26-27-000021 (lot 6S-0919, serial MPCB-0919-0001, bill `6S/INV/0919/002`) → FINALIZED, batch 54b5c36b. **Still under the 16 Sep `ELECTRICALS → ELE` rename.** Codex's #4 (`5b903eb`) fixes the *import* and ships `database/postgresql/reconcile-legacy-item-categories.sql` for existing rows — "not applied to the live database". FABRICATION / REFRIGERATION are still long codes here, so 0 of their items can be received until it is run. |
| 12 | QC queue + inspect | SESS-33 | **YES\*** (SUPPORT role still in place) | Queue shows GRN-000021 lot 1 with `EFFECTIVE_POLICY`; both policies bind (VISUAL on the item, INSULATION_RESISTANCE on ELE — the first attempt with VISUAL alone was refused "requires samples 1 through 1", correctly). QCI-20260919-000021 **ACCEPTED**, serial MPCB-0919-0001 accepted. #16 closed: the policy list carries `ItemCode`. The GRN read for QC is *not* in any migration of this set (112 grants Purchase vendor view + TD job-order view only), so SESS-33 still relies on `STORES_ASSISTANT` SUPPORT — the role-end command was handed to the user; the flag stays until the read is re-tested without it. |
| 20 | Component Fitment / Actual BOM | SESS-25 view | **YES** | JO-000031 re-read after #12: MPCB-0916-0001 **LANDED_ACCEPTED ₹1,250.00** (was 1,475). Offer vs actual (Estimated BOM rev 1): baseline ₹2,500 / actual ₹1,250 / **variance −₹1,250.00**; Plan vs actual identical. On screen and API. |
| 24 | Opening Stock ceremony | SESS-41 count, SESS-14 value, SESS-01 authorize | **YES\*** (was NO) | SESS_PROPRIETORSHIP. #14 closed (`ddb6340`): upload 200 for every master. Bad workbook → COMPLETED_WITH_ERRORS 1 rejected; good workbook uploaded **through the screen** → 3 created / 0 rejected, batch id auto-filled. **Record count refused**: "Every imported Opening Stock row requires one effective AVAILABLE rack location" — GEN-02/03 have none and **none can be created (finding 18)**. Re-run with all three rows on TRIAL-C02-GEN-01: `776e61b7` COUNTED (SESS-41, 300 qty / ₹29,925) → **VALUED** (SESS-14, screen) → **POSTED** (SESS-01, screen), posting batch 5ce7bb57, three FIFO layers. Refusals proven: same person (SESS-14 authorize → 403), stale version (v0 → 409 "stale version or invalid status"), plus movements / import / authority from 18 Sep. |
| 25 | Reports — machine dossier | SESS-14 | **YES** | MC-0916-001: landed value **₹1,250.00**, totals 1,250.00 — the second half of the 18 Sep prediction. |
| 26 | **Vendor Bills** (new) | SESS-14 | **YES** | `/accounts/vendor-bills`: list (1 → 2 bills), **Raise from GRN** looked up GRN-26-27-000021 by number (#11 closed — Accounts reads GRNs), pre-filled bill no. / date from the GRN, payable defaulted 1,475 (1,250 + 18 %), raised → DRAFT MATCHED → **Accept** with reason → ACCEPTED v1, **Landed rate ₹1,250** on the line. Closes #13. |
| 27 | **Vendor Payments** (new, item 5) | SESS-14 | **YES** | `/accounts/vendor-payments`: payables 2 (₹4,425); **Record payment** — vendor picked, bill 0919/002 "Full", PNG bank advice uploaded through the screen (`bank-advice:80ee6cb3…`), reference `HDFC-NEFT-0919-77213` → **VPAY-2026-000001** ₹1,475, payables → 1. **Record advance** — PO-26-27-000011 (available 2,950), ₹1,000, bank advice `bank-advice:a7db8a94…` → **VADV-2026-000001**. Vendor position: advance 1,000 / bills 2,950 / net payable 1,950. |
| — | Master-data round trip (item 6) | SESS-01 | **YES** | Export → re-import unchanged: UOMs 15/15, customers 143/143 (the customer one **through the screen**), vendors 90/92 — **SESS-V-0004 and SESS-V-0028 share PAN AADFF4398M and legal name** (data, not code; errors.xlsx 200, 2 rows). |

**Count: 27 lines on the grid, 25 / 27 proven — 22 clean, 3 under flags
(11 category rename, 12 QC read via SUPPORT, 24 GEN-01 only). The −1,250
prediction held on the Actual BOM, on the screen and in the dossier. Vendor
bill and vendor payments now exist as screens and ran end to end as Accounts.
Items 3, 4, 5, 6 and line 24 are closed; lines 7–10 moved from YES\* to YES.**

## Backend findings

Closed today by `f94f332` (witnessed): #5, #7 (Purchase vendor read), #8 (rule
existed), #9 (`StoredSerialNumber` / `FittedQuantityBase` adopted), #11, #12
(−1,250), #13 (screen built), #14, #16. **#1 is closed on a clean clone only
(see 19 below). #2 could not be witnessed here — 75 was already applied.**

Still open: #3 for the TSM role (17 below), #4 until the reconcile script runs,
#7 for QC's GRN read (12 above), #10 was closed 18 Sep, #15 not reached.

New today:

17. **Technical verification by `TECHNICAL_SUPPORT_MANAGER` 500s in the
    history guard.** SESS-04 in SESS_PVT_LTD, VQ-26-27-000021: passes
    `RequireRole(TechnicalVerification)`, passes
    `QuotationTechnicalScope.AccessibleRfqs`, inserts the verification, then
    `rev869b_guard_history_insert()` line 46 raises 42501
    `rev869b_history_actor_binding` ("History actor, role, correlation and
    remarks must match the current controlled transition") on the status
    history row — surfaced as 500. SESS-01 (TD) succeeds on the same quotation
    with the same request. The guard is a composite (`rev869b_command_context_valid`,
    identity mapping, role assignment, remarks); which clause fails for TSM
    needs the database. Repro: `POST /purchase/quotations/VQ-26-27-000021/technical-verifications`
    as SESS-04 before SESS-01's verification landed (13:12 local).
18. **No AVAILABLE condition location can be created for any seeded bin.**
    `Rev869AConfigurationEndpoints.cs:497` requires
    `NormalizeCode(rack.MaterialCondition) == condition`; the rack master's
    vocabulary is `Accepted` / `Rejected` (`GET /inventory/rack-bins`), the
    condition vocabulary is `AVAILABLE` / `QC_HOLD` / …, so every
    `POST /rev869a/configuration/warehouse-condition-locations` for
    `AVAILABLE` returns 409 "Warehouse/RackBin/condition mapping is invalid".
    Only the migration-seeded GEN-01 locations exist. Consequence: opening
    stock (and any receipt to another bin) is limited to one bin per
    warehouse. Also: `STORES_MANAGER` holds create on condition locations but
    not `masters.rack-bins:view` (403) — it cannot obtain the `RackBinId` the
    request needs (reachability).
19. **#1 in a second form — the LF fix does not reach an upgraded Windows
    checkout.** `159822d` sets `Migrations/** text eol=lf`; git applies the
    attribute only to files a checkout rewrites, so 241 files under
    `Migrations/` and `Shared/` stayed `w/crlf` here with `git status` clean.
    Migration 108 (`ActualBomLandedRateValuation.ReplaceOnce`, line 127)
    normalises its search text to LF but searches
    `ImmutableLandedCostAdjustmentsSql.Up` compiled from the CRLF copy →
    "Actual BOM fitment valuation baseline changed." Fixed locally by removing
    and restoring the 241 tracked files (zero content change), rebuild,
    resume; 108–113 then applied. `MigrationSqlLineEndingTests` reads the same
    working copy so it cannot catch this. One-line fix: normalise both sides
    in `ReplaceOnce`; otherwise document `git rm -r --cached . && git reset
    --hard` after pulling.
20. **Vendor master duplicate identity:** SESS-V-0004 (FLUIDOZE CONTROL
    SYSTEMS LLP) and SESS-V-0028 carry the same PAN `AADFF4398M` and legal
    name; the import refuses both rows (`DUPLICATE_IDENTITY`). Data, for the
    TD team — but it means the vendor export can never round-trip clean until
    one is merged or ended.

Observation, not a defect: an advance recorded *after* the PO's bill was
accepted (VADV-2026-000001 against PO-000011) is not adjusted against that
bill (Advance adj. 0 on 6S/INV/0916/001); adjustment happens at acceptance.
Vendor position shows it netted (1,950). Worth a sentence in the Accounts
manual.

## Frontend changes today (feature/frontend)

- **Vendor Bills** `112d1c6`→`43f4264`: list, raise-from-GRN modal (GRN by
  number, lines defaulted from the GRN, charges with RECOVERABLE_GST flagged
  automatically), detail with expected vs billed, match, allocated charges,
  `LandedUnitRate`, Decision (accept / reject / reverse). Nav "Accounts".
- **Vendor Payments** `8c8e97b`: payables (overdue filter), advances (record
  against issued PO with headroom, reverse while unadjusted), payments
  (allocations across the vendor's outstanding bills, "Full" shortcut, amount
  = sum), vendor positions; bank advice upload returns the evidence key.
- **Adopted reads** `112d1c6`: issue lines carry `StoredSerialNumber` /
  `FittedQuantityBase` (return scan matches its own line; detail subtracts
  the server's fitted qty); QC policy cards show `ItemCode`.

## Run log

- 11:38 user: checkout −, ff to f94f332, API stopped, pre-113 dump + globals.
- 11:44 build; 11:50 `database update` → 107 ok, **108 failed** (19 above).
- 11:52 241 CRLF working copies restored as LF; 11:56 rebuild; 12:00 108–113
  applied, 113/113; user: provision RECONCILED, status VERIFIED.
- 12:05 API from the worktree; 12:08 Actual BOM −1,250 (API + screen); 12:10
  dossier 1,250.
- 12:15 vendor bill screen read path; 12:20 SESS-14 GRN read 200 (#11).
- 12:40 PR-000009 → 12:47 RFQ/VQ-000021; 12:50 SESS-04 verify 500 (17),
  SESS-01 verify ok; 12:52 CMP/PO-000021; 12:55 GE/GRN-000021; 12:58
  QCI-20260919-000021 ACCEPTED.
- 13:00 vendor bill 6S/INV/0919/002 raised + accepted through the screen.
- 13:10 payments screen: VPAY-2026-000001, VADV-2026-000001.
- 13:20 opening stock PROP: import 200 (#14), refusal on GEN-02/03 (18),
  GEN-01 batch → COUNTED / VALUED / POSTED through the three screens.
- 13:30 master round trip: uoms 15/15, vendors 90/92 (20), customers 143/143
  via the screen.
