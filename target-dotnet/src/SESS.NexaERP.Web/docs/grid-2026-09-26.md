# Grid re-run: 26 September 2026, screens through local Keycloak

Local database `sess_nexa_erp` (130/130). API at `origin/main` `b7f64a9` in OIDC mode, as
`nexa_erp_runtime`. Local HTTPS Keycloak on 8444. Every step below went **through the screen**,
signed in through Keycloak, not through development sign-in. Login proof: `login-proof-2026-09-26.md`.

| # | Line | Result | Evidence |
|---|---|---|---|
| 12 | QC read + inspect by SESS-33, **no support role** | **YES** | SESS-33 (QC_MANAGER only; STORES_ASSISTANT support ended 24 Sep) sees GRN-26-27-000031 lot 1 in the QC queue. The FAB category had no QC policy, so the lot could not be finalized. SESS-33 created policy `8483d446` through the QC policy screen (category FAB, VISUAL pass/fail, 1 sample, effective 2026-09-24); SESS-01 (TD) approved it through the API with development sign-in, because SESS-01's Approvers login needs OTP enrolment. SESS-33 then finalized **QCI-20260926-000031: 10 accepted, 0 rejected, VISUAL PASS**, stock posting batch `1cfab96c`, to TRIAL-C01-GEN-01. |
| 11 | GRN of a FAB item through the screen, SESS-35 | **Not run** | YES through the API on 24 Sep. Every issued PO in SESS_PVT_LTD is fully received, so a screen GRN needs a new PO. PO and comparison approval route to SESS-14, whose Approvers login waits for OTP enrolment on a phone. |
| new | Machine DC screen, SESS-35 | **YES (partly)** | Job list shows JO-000031 / MC-0916-001. Dispatch form opens with nothing pre-selected; purpose narrows to CUSTOMER_PO_BASED for non-returnable; the confirmation names everything that cannot be changed. The server answers 409 "This job already has a machine DC", shown as such. Signed view of DC-2026-0001 renders the signature, file hash and the download-permission hint. **Signature capture not witnessed**: no dispatched-unsigned DC exists locally. |

## Go-live checklist item confirmed again

**Every category the team will receive needs an approved QC policy before its first inspection.**
FAB had none on 26 Sep, so a finalized FAB receipt sat in QC hold with "No policy" until one was
prepared by QC and approved by the TD. Add FAB, REF and every other received category to setup.

## Backend gaps found by the DC screen (for the TD)

1. The dispatch job list includes jobs that already have a DC; the dispatch is then refused 409.
2. There is no endpoint to list DCs or find a job's DC. The screen remembers DCs opened on the
   browser as a stopgap.
3. The DC view carries `JobOrderId` but no job order number.

## Afternoon: three backend features that had no screen

Built and wired 26 Sep. Local Keycloak sign-in throughout.

| Screen | Result | Evidence |
|---|---|---|
| **Stock Adjustment** (A2), `/stores/stock-adjustments` | **YES, end to end** | SESS-35 (STORES_EXECUTIVE) recorded ADJ-SESS_PVT_LTD-26-27-000011: COUNT_VARIANCE, −1 ACMS-10X10 at TRIAL-C01-GEN-01, FIFO ₹14.75, period INV-2026-09, and submitted it with a reason. The server named **Stores Manager** (band under ₹5,000); the screen told SESS-35 their own decision would be refused. SESS-41 (STORES_MANAGER, linked to local Keycloak today) approved through the in-page confirmation; **POSTED**, batch `34a1341e`. |
| **Inventory Periods**, `/accounts/inventory-periods` | **Built; not witnessed on screen** | Needs the CFO (SESS-02) signed in through Approvers with an authenticator code. For the test above, INV-2026-09 (1-30 Sep) was opened as SESS-02 through the API. |
| **Notifications** bell + `/notifications` | **YES (empty inbox)** | Bell in the header, inbox loads for SESS-35 (none). The only three notification types the backend raises today are unused-material overdue, QC ageing overdue and non-returnable machine dispatch (to the MD). |

Observation for the TD: the removal was valued at ₹14.75, which is 10 × ₹12.50 plus 18 % GST divided by 10: GRN-000031 has no accepted vendor bill yet, so its FIFO layer still carries the PO's GST-inclusive rate.

Stock adjustment gaps (backend): serialized identity change is not built (use wrong-serial-out + right-serial-in); a backdated adjustment needs a `BackdateEvidenceId` with no upload behind it; the 403 for a wrong role drops the server's "Required role" text; the server's "future date" check uses the UTC date, so 00:00-05:30 IST today counts as tomorrow.
