# Grid re-run — 24 September 2026, lines 11, 12 and 24

Local database `sess_nexa_erp` at 130/130 migrations. API from the backend worktree at
`origin/main` `2149d27` (includes `7003c02`), running as `nexa_erp_runtime`. Company
`SESS_PVT_LTD`. Development sign-in was used for the API calls. Since `a95de9f` the screens accept
only Keycloak sign-in, and local Keycloak is not running yet, so every step below went **through the
API as the right person, not through the screen**. Each result is marked accordingly.

| # | Line | Result | Evidence |
|---|---|---|---|
| 11 | GRN of a FAB item | **YES (API)**, screen step pending login | Full chain on the canonical FAB category, no workaround role: RFQ-26-27-000031 → vendor SESS-V-0001 invited (SESS-15) → VQ-26-27-000031, 10 × ₹12.50, CGST 11.25 + SGST 11.25 = ₹147.50 (SESS-15) → TechnicallyCompliant (SESS-01, TD, no SUPPORT role) → CMP-26-27-000031 recommended (SESS-15), approved (SESS-14), route DEPARTMENT_ONLY → PO-26-27-000031 created/submitted (SESS-15), approved (SESS-14), issued (SESS-15) → GE-26-27-000031 FINALIZED (SESS-35) → **GRN-26-27-000031 FINALIZED (SESS-35)**, ItemCategoryCode `FAB`, 10 NOS to QC hold location `71000000-0000-0000-0009-000000000002`. On 16 Sep no FAB item could be received at all. |
| 12 | QC read by SESS-33 without STORES_ASSISTANT | **Not yet run: 25 Sep** | SESS-33's SUPPORT assignment `cc4ea053` was ended through `POST /employees/SESS-33/roles/{id}/end` by SESS-01, EffectiveTo 2026-09-24, EndedAt 06:29 UTC. The API refuses a past EffectiveTo and the end date is inclusive, so the role is in force until midnight UTC. `QC_MANAGER` holds `inventory.grn` view only (migration `20260920120000_QcGoodsReceiptRead`). GRN-26-27-000031 is now in QC hold and is the lot to inspect. |
| 24 | Opening stock, SESS_PROPRIETORSHIP | **Cannot be re-run: stands YES from 19 Sep** | Opening stock is once per company and refuses after any stock movement. SESS_PROPRIETORSHIP was POSTED on 19 Sep (batch `5ce7bb57`). The refusal is correct behaviour. Finding 18 closed on 23 Sep. |

## Setup data created on the way

- **GST rule `e9a10d1f`**: IN_GST, HSN 73181500, TN→TN, REGULAR, 18 % (9 + 9), effective
  2026-04-01, open-ended. Created by SESS-14 and approved by SESS-02 (MANAGING_DIRECTOR). Without it
  no FAB fastener quotation can be priced. **This is go-live checklist item #8 again:** every
  HSN/state pair the team will buy needs an approved rule before its first quotation.

## Still open

- Line 11 through the GRN **screen**, and line 12 through the QC **screen**, once local Keycloak
  sign-in works.
- SESS-16 `TECHNICAL_ENGINEER` SUPPORT `21136f14` is still active, awaiting the manager's
  decision. Line 11's technical verification above did not use it.
