# Frontend grid — 16 September 2026

Backend: origin/main `591fe42` (dd992ea + 3), local DB migrated 74 → 103 today
(75 as postgres, 76 → 103 as `nexa_erp_migration`; provision RECONCILED,
status VERIFIED). Frontend: feature/frontend. Company: SESS_PVT_LTD. API as
`nexa_erp_runtime` on :5000, frontend on :5175. Every action below was driven
through the screen unless the line says "API".

**E2E** = runs end to end, as the right person, against the real API:
`YES` / `YES*` (passes only under a flagged dev workaround — a line waiting for
the real fix, not a passing line) / `NO` / `NOT RUN`.

| # | Screen | Logins used | 16 Sep E2E | Evidence / blocking reason |
|---|---|---|---|---|
| 1 | Item master | SESS-01 | **NO** | List 1388, detail OK. **Edit refused for every item**: `InventoryEndpoints.cs:231` requires the item's UOM to have `QuantityPrecision == 6`; only `PAIR` qualifies, 0 / 1388 items use it. Backend rule vs migrated UOM master. |
| 2 | Vendor master | SESS-01 (SESS-15 blocked) | YES | List 91, detail, create SESS-V-0092, edit. Qualification e9516138 verify SESS-01 / approve SESS-02 (API, 200/200) — **10 Sep defect 3 closed**. Seed gap: `PURCHASE_EXECUTIVE` / `PURCHASE_MANAGER` have no `masters.vendors` view → Purchase cannot open the vendor master and the RFQ vendor picker 403s. |
| 3 | Customer master | SESS-01 | YES | List 143, detail, edit CUST-00000001 saved. |
| 4 | Employee master | SESS-12 | YES | List 45 (Active default), detail SESS-01. |
| 5 | Purchase Requisition | SESS-15 raise, SESS-14 verify+approve, SESS-01 approve | YES | PR-2026-27-000008, route DEPARTMENT_THEN_TD. Frontend fix: PR create now sends `Idempotency-Key` (backend c3e4902, 13 Sep). Logins corrected: PRODUCTION_MANAGER has no PR create grant. |
| 6 | RFQ | SESS-15 | YES | RFQ-26-27-000011 Draft → Issued after inviting SESS-V-0001 (invite via API — the screen's vendor picker is blocked by the line-2 grant gap). Screen shows lines and Issued. |
| 7 | Quotation | SESS-15 record, SESS-16 verify | **YES\*** | VQ-26-27-000011 recorded through the screen, then TechnicallyCompliant. Frontend fixes: submission source `EMAIL_RECEIVED|PHYSICAL_RECEIVED`, registration type canonical `REGULAR`. Seed gap: no GST rule existed — HSN 85362010 TN/TN 18 % created SESS-14 / approved SESS-02 (go-live checklist). **Workaround**: technical verification is unreachable for every seeded employee (RFQ department is always PURCHASE; TD/TSM scoped elsewhere; cross-scope hard-disabled; PURCHASE scope needs department membership) — SESS-16 (Purchase-scoped stores assistant) given `TECHNICAL_ENGINEER` SUPPORT as SESS-12. Fix (a) with Codex. |
| 8 | Comparison | SESS-15 build/recommend, SESS-14 approve | YES\* | CMP-26-27-000011, ₹2,950, route DEPARTMENT_ONLY → Approved. Depends on 7's workaround. |
| 9 | Purchase Order | SESS-15 create/submit/issue, SESS-14 approve | YES\* | PO-26-27-000011 Draft → PendingApproval → Approved → Issued 13:11. |
| 10 | Gate Entry | SESS-35 | YES\* | GE-26-27-000011 draft (ISO checks, 2 delivered) → FINALIZED. |
| 11 | Goods Receipt | SESS-35 | **YES\*** | GRN-26-27-000011 with 2 scanned serials → FINALIZED, stock to QC hold. **Workaround**: `EfGoodsReceiptService.cs:166` refuses any item whose category code is not exactly 3 characters; master categories are `ELECTRICALS`, `FABRICATION`, `REFRIGERATION`, `TRIAL-*` — only `PLC` (0 items) qualifies, so **0 / 1388 items can be received**. Renamed category `ELECTRICALS` → `ELE` as SESS-01 (the rack master already uses ELE/FAB/FAS/MEC/PLC/REF, so the item-category import is the mismatch). |
| 12 | QC queue + inspect | SESS-33 | NOT RUN | Queue shows the lot. Inspect page 403: `QC_MANAGER` has no `inventory.grn` view, and a serialized inspection needs the GRN's serial ids (no QC endpoint returns them). Seed gap; workaround (QC_HEAD SUPPORT on SESS-33) pending the user's shell. |
| 13 | Concession | SESS-33, SESS-01 | NOT RUN | behind 12 |
| 14 | Stock Check | SESS-15 | YES | PR-2026-27-000008 checked at TRIAL-C01-GEN-01 → NotAvailable, 4 handed to Purchase. |
| 15 | Material Issue Request | SESS-25 raise, SESS-41 approve | YES | Job-backed MIR-SESS_PVT_LTD-26-27-000051 against JO-000031 → APPROVED — the 10 Sep "CPO line per component" block is gone. Lookup gap: SESS-01 (TD) gets 403 on the job-order list so cannot raise a job-backed MIR. |
| 16 | Material Issue (by scan) | SESS-35 | NOT RUN | needs accepted stock (behind 12) |
| 17 | Material Return | SESS-35, SESS-25 | NOT RUN | behind 16 |
| 18 | Customer PO | SESS-01 | YES | CPO-00003 (CPO-GRID-0916-003, Machine line TRIAL-ITEM-009, ₹1,18,000). |
| 19 | Job Order | SESS-25 create, SESS-14 verify, SESS-33 FAT | YES | JO-SESS_PVT_LTD-26-27-000031 PENDING_ACCOUNTS → OPEN. FAT readiness reachable as SESS-33 (10 Sep fallback verified) and correctly refused: "requires a generated Actual BOM from confirmed fitment". |
| 20 | Component Fitment / Actual BOM | SESS-25 | NOT RUN | needs issued stock (behind 16) |
| 21 | Estimated BOM | SESS-04 create/submit, SESS-01 approve | YES | EBOM-…-000031 DRAFT → SUBMITTED → APPROVED. Stuck EBOM-…-000011 returned to DRAFT with the new **Return to draft** button (backend action existed under the Reject grant; screen lacked it). |
| 22 | Production BOM | SESS-25 create/submit/pin, SESS-01 approve | YES | PBOM-…-000021 → APPROVED → pinned to JO-000031 (pin is Production's, TD refused — correct). |

**Count: 16 / 22 proven — 11 clean, 5 under flagged workarounds (7–11). 1 NO (item edit). 5 NOT RUN, all behind the QC grant gap (12). Last verified 10 Sep: 7 / 22.**

## Backend findings for Codex (in chain order)

1. **Migration 89 breaks on Windows checkouts** (`20260913095000_PurchaseOrderSupersedeHistory.cs`): the `Patch` raw-string literal inherits CRLF; the post-check normalises `prosrc` to LF and never finds it. Proven by converting `Migrations/*.cs` to LF (zero content diff) and rerunning 89 → 103 first try. Fix: normalise the literal or `eol=lf` in `.gitattributes`.
2. **Migration 75 needs superuser** on a `WasInstalled=true` database (`REASSIGN OWNED`) — 15 Sep finding, still true.
3. **Quotation technical verification unreachable** — see line 7. Fix (a): scope-check the verifier's own department / item category, not the RFQ's.
4. **GRN 3-character category rule vs item-category import** — see line 11. Either relax `EfGoodsReceiptService.cs:166` or migrate item categories to the rack codes (ELE/FAB/FAS/MEC/PLC/REF).
5. **Item edit unreachable** — see line 1 (`InventoryEndpoints.cs:231`, UOM precision 6).
6. **Seed / grant gaps**: Purchase roles lack `masters.vendors` view (line 2); `QC_MANAGER` lacks `inventory.grn` view and no QC endpoint exposes serial ids (line 12); TD cannot list job orders (line 15); PRODUCTION_MANAGER cannot create PRs (line 5, may be intended).
7. **Go-live checklist**: a GST tax rule per HSN/state pair must exist before the first quotation (line 7).

## Frontend changes today (feature/frontend)

- Quotation: submission source and vendor registration type use the service's canonical values.
- Purchase Requisition create sends `Idempotency-Key`.
- Estimated BOM detail: **Return to draft** for SUBMITTED revisions (Reject grant).

## Run log

- 16 Sep 12:20 pre-75 dumps taken (database + globals) for Codex; 12:33 75 applied as postgres; 12:55 76 → 88 as migration principal, 89 failed (CRLF); 13:00 LF rebuild, 89 → 103 Done; provision RECONCILED, status VERIFIED; API up as runtime; grid from 13:15.
- Dev data created: tax rule 95d07e05 (IN_GST 85362010 33/33 REGULAR 18 %), category ELECTRICALS renamed ELE, SESS-16 +TECHNICAL_ENGINEER (SUPPORT), VQ/CMP/PO/GE/GRN-26-27-000011, PR-2026-27-000008, CPO-00003, JO-000031, EBOM-000031, PBOM-000021 (pinned), MIR-000051, vendor SESS-V-0092.
